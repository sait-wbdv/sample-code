// Create a starter array with `name = [a, b, c]`
const dogs = ['pug','puppy','husky','wolf'];

console.log(dogs);

// Change an item in an array with `name[index] = y`
dogs[3] = 'bulldog';

console.log(dogs);